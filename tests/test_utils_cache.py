"""Test utils_cache."""

from dash_charts import utils_cache

# FIXME: TEST THE FILE
# pretty_dump_json(filename, obj)
# initialize_cache()
# match_url_in_cache(url)
# store_response(prefix, url, obj)
